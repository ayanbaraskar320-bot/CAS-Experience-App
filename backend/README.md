# Backend

This folder is reserved for server-side work in the CAS Experience workspace.

Suggested structure when backend work begins:

```text
backend/
  src/
  routes/
  services/
  data/
  README.md
```

For now, the application is frontend-only. Keep API and server logic here when it is added later.